# models/

Trained weights live here, gitignored (see `.gitignore` — track via DVC or a release asset instead of git).

- `unet_v1.pt` — U-Net trained on BBBC038/DSB2018 nuclei (`train/train_unet.py`). Not yet trained.
